# OpenAI API Configuration
VITE_OPENAI_API_KEY=your-openai-api-key-here

# Note: For Vite applications, environment variables must be prefixed with VITE_ to be accessible in the browser
# For Node.js scripts (like tests), use OPENAI_API_KEY without the VITE_ prefix